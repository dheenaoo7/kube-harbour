apiVersion: v1
kind: Service
metadata:
  name: affiliate-ui
  labels:
    app: affiliate-ui
spec:
  selector:
    app: affiliate-ui
  type: NodePort
  ports:
  - port: 8088
    targetPort: 8088
    nodePort: 30000